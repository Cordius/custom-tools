set logfile sandbox/mail/fetchmail.log

poll imap.xxxxx.com
proto imap
port 993
user 'xxxx@xxxx' with password 'xxxxxx' is '<Linux User Name>' here # <Linux User Name>指的是你当前Linux系统的用户名
mda "/usr/bin/procmail -d %T"
keep
options
ssl

set daemon 10 # 10s在后台轮询一次查找有无新邮件要收
